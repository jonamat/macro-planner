{
  "name": "macro-calculator",
  "version": "1.0.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "concurrently \"yarn workspace @macro-calculator/shared dev\" \"yarn workspace @macro-calculator/server dev\" \"yarn workspace @macro-calculator/client dev\"",
    "dev:server": "yarn workspace @macro-calculator/server dev",
    "dev:client": "yarn workspace @macro-calculator/client dev",
    "build": "yarn workspace @macro-calculator/shared build && yarn workspace @macro-calculator/server build && yarn workspace @macro-calculator/client build",
    "test": "yarn workspace @macro-calculator/shared test && yarn workspace @macro-calculator/server test && yarn workspace @macro-calculator/client typecheck && yarn workspace @macro-calculator/client test",
    "migrate": "yarn prisma migrate deploy --schema ./prisma/schema.prisma"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "@types/node": "^22.5.0",
    "concurrently": "^9.0.1",
    "jest": "^29.7.0",
    "ts-jest": "^29.2.5",
    "ts-node": "^10.9.2",
    "tsx": "^4.15.7",
    "typescript": "^5.9.2"
  }
}
